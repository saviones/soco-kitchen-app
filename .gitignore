# build artifacts / superseded originals — not part of the app
assets/menu-source/contact-sheet.jpg
assets/food/screenshot-1.png
assets/food/screenshot-2.png
assets/food/screenshot-3.png
# licensed stock photo from the website — don't redistribute in a public repo
assets/food/beignets-shutterstock.jpg
Thumbs.db
desktop.ini
# secrets — Toast API credentials live only in local env files, never in the repo
.env
.env.*
.dev.vars
backend/.dev.vars

# dev ledger state — local test vouchers, never commit
backend/.ledger-dev.json
backend/.ledger-dev.tmp

# operating figures — public repo, keep revenue-derivable numbers out
LAUNCH.local.md
